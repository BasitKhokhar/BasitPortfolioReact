[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: let React Router handle client-side routes
# (existing static files in dist/ are still served first)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
